"use client";

import { HighlightedText } from "@/components/highlighted-text";

import { useState } from "react";

export default function Home() {
	const [clicked, setClicked] = useState(false);

	return (
		<main className=" text-zinc-800 p-8 flex gap-4">
		

    
		</main>
	);
}
